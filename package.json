{
    "name": "swtl-cms",
    "version": "0.0.1",
    "description": "A SPA CMS built with SWTL",
    "main": "index.js",
    "scripts": {
      "start": "cp src/index.html . && cp src/vendor/*.js . || true  && esbuild src/sw.js --bundle --outfile=sw.js --watch --format=iife",
      "build": "node build.js",
      "dev": "node build.js --watch"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
      "@octokit/rest": "18.0.15",
      "element-internals-polyfill": "^1.3.12",
      "swtl": "^0.2.1",
      "urlpattern-polyfill": "^10.0.0"
    },
    "devDependencies": {
      "esbuild": "^0.19.9"
    }
  }
